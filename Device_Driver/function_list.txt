########## led_driver ##########

static int led_open(struct inode *minode, struct file *mfile) -> .open

static int led_release(struct inode *minode, struct file *mfile) -> .release

static ssize_t led_write(struct file *mfile, const char *gdata, size_t length, loff_t *off_what) -> .write





########## step_motor_driver ##########

static int sm_open(struct inode *minode, struct file *mfile) -> .open

static int sm_release(struct inode *minode, struct file *mfile) -> .release

static ssize_t sm_write(struct file *mfile, const char *gdata, size_t length, loff_t *off_what) -> .write
gdata = 스텝 모터 모드(정방향 = '1', 역방향 = '2')